# 验证

## 命令

```powershell
mvn test
```

## 结果

- `{command}`：`{passed/failed/not run}`

## 静态检查

- 依赖方向：`{result}`
- public DTO 位置：`{result}`
- client/server 分离：`{result}`
- 下游调用配置化：`{result}`
- 接口兼容策略：`{result}`
- 契约测试或替代校验：`{result}`

## 结论

`{conclusion}`
